# Vercel Blob Storage Token (Required for storage)
BLOB_READ_WRITE_TOKEN=

# Admin Token (Required for accessing admin panel)
ADMIN_TOKEN=

# Encryption Key (Required for securing PII)
# Must be at least 32 characters long
ENCRYPTION_KEY=
